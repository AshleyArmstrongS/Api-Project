type Query {
  info:             String!
  herd:             [Animal!]!
  animal(id: ID!):  Animal!
}
type Animal {
    tag_number:     Int
}

enum MF {
  M
  F
}
type Mutation {
  createAnimal(tag_number:Int!) : Animal
}
